package coreConcepts

import (
	"fmt"
)

func Exercise(){
	// write a program the takes numbers - from terminal, .txt file and computes the average
	// write a program that takes a string and replaces all occurences of a word with another
	// write a program that takes a file and counts the unique words in the file, and sort the to tell what the top 3 most popular words were
	fmt.Println("This is an exercise function")
}