package coreConcepts

import (
	"fmt"
)

func Exercise(){
	// write a function the takes numbers - from terminal, .txt file and computes the average
	// write a function that takes a string and replaces all occurences of a word with another
	fmt.Println("This is an exercise function")
}